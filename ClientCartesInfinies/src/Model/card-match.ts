export class CardMatch {
  constructor(public id: number, public name: string, public attack: number,
              public defense: number, public image: string, public attackedThisTurn: Boolean) {}
}
